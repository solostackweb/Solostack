// Welcome documents no longer have an acknowledgement flow — they are simply a
// branded greeting/onboarding document. This module is intentionally empty
// (the sandbox does not permit deleting the file).
export {};
