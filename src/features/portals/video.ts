/**
 * Deprecated. Built-in Jitsi rooms were removed because the public
 * meet.jit.si instance now requires a logged-in moderator to start a room
 * (anonymous rooms get stuck "waiting for a moderator"). Portal meetings now
 * use a freelancer-supplied Google Meet / Zoom link instead.
 *
 * Intentionally left as an empty module to avoid a dangling import path.
 */
export {};
