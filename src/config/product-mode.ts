/**
 * Temporary launch mode for Stackivo's private early-access community.
 *
 * Keep this as the single reversible switch. Billing records, usage counters,
 * and checkout infrastructure stay intact, while product entitlements and
 * customer-facing pricing surfaces remain disabled during testing.
 */
export const EARLY_ACCESS_MODE = true;

